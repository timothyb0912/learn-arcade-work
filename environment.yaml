name: learn-arcade-work
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - python==3.8.5
  - pip
  # Above, list any packages that MUST be installed via conda
  - pip:
    # Install all pip-installable packages
    - -r file:requirements.txt
